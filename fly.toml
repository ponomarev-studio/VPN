app = 'vpn-ps'
primary_region = 'ams'
swap_size_mb = 256

[mounts]
  source = "vpn_data"
  destination = "/data"

[[restart]]
  policy = "always"

[[vm]]
  cpus = 1
  memory_mb = 256
  cpu_kind = 'shared'
  size = 'shared-cpu-1x'

[deploy]
  strategy = "immediate"
